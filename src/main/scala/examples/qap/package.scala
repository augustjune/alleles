package examples

package object qap {
  /**
    * Combination of all facilities with corresponding locations,
    * where each pair of element and its position in vector is
    * understood as number of the facility with corresponding location
    */
  type Permutation = Vector[Int]
}
